[settings]
class = CPayout
fields = \
    address address|\
    blknum bn|\
    blknum txid|\
    blknum logid|\
    string type|\
    double amount
includes = etherlib.h
display_str = address, bn, txid, logid, type, amount
